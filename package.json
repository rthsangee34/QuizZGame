{
  "name": "quizverse-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "node dev.js",
    "install:all": "npm install --prefix backend && npm install --prefix frontend"
  }
}
